.container
  .page-header.text-center
    %h1 Our Equipment
  %ul(ng-model="equipments" ng-if="equipments")
    %li(ng-repeat="e in equipments")
      %a(ng-href="{{ e.image }}")
        %img(ng-src="{{e.thumb}}")
