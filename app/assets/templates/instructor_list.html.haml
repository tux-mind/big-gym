.media(ng-repeat="i in instructors" ng-href="#/instructors/{{i.id}}")
  .media-left
    %a(ng-href="#/instructors/{{i.id}}")
      %img.media-object.img-circle(ng-src="{{i.image}}" ng-attr-alt="{{i.full_name}}")
  .media-body
    %h4.media-heading
      {{ i.full_name }}
    {{ i.summary }}
